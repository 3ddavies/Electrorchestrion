async def play(nar):
	tracks =
	channels =
	for i in tracks:#for each track
		for j in channels:#for each channel in a track
			pass
		pass

	return

pint(htp(p))

async def pint(p):#pin toggle
	pass

first_note=C
async def htp(h):# hex to pin.
	"""
	translate a hex note value into a pin number on control board.
	"""
	pass